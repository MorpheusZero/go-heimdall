CREATE TABLE persons_schema (
    first_name text,
    last_name text,
    email text
);

INSERT INTO persons_schema (first_name, last_name, email) VALUES ('first','last','email');

INSERT INTO persons_schema (first_name, last_name, email) VALUES ('second','last','emailagain');
